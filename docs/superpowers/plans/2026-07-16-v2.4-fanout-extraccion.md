# v2.4 — Fan-out de variantes + extracción estructurada — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Cada búsqueda dispara 3 variantes de query en paralelo, une los resultados deduplicados por `supplierKey`, y el prompt fuerza precios/dirección a campos estructurados (con salida estructurada de la API como mejor esfuerzo).

**Architecture:** Todo el cambio vive en `src/sourcing/supplierSource.ts`: una función pura `buildQueryVariants` genera 3 plantillas fijas; `search()` las corre con `Promise.allSettled` (una variante caída no tumba a las demás) y deduplica con `supplierKey` de `directory/store`. `SearchBudget` gana `maxVariants` (default 3) y pierde `maxEmptyRetries` (el fan-out lo reemplaza). El contrato externo de `/api/buscar` no cambia.

**Tech Stack:** TypeScript ESM, vitest (cliente Anthropic mockeado), @anthropic-ai/sdk ^0.105.0 (`claude-opus-4-8`, thinking adaptive + `output_config.effort`), zod.

**Spec:** `docs/superpowers/specs/2026-07-16-v2.4-fanout-extraccion-design.md`
**Rama:** `feat/v2.4-fanout-extraccion` (ya creada, con el spec commiteado).

**Comandos del repo** (siempre con prefijo `rtk`):

- Tests de un archivo: `rtk vitest run src/sourcing/supplierSource.test.ts`
- Suite completa: `rtk pnpm test`
- Typecheck: `rtk pnpm typecheck` · Lint: `rtk pnpm lint` · Formato: `rtk pnpm format`

**Reglas del proyecto:** comentarios en español, identificadores en inglés; TDD estricto (test → RED → impl → GREEN → commit); NO pushear ni abrir PR sin orden explícita del usuario.

---

## Estructura de archivos

| Archivo | Acción | Responsabilidad en este plan |
|---|---|---|
| `src/sourcing/supplierSource.ts` | Modificar | `buildQueryVariants`, fan-out en `search()`, `SearchBudget` sin `maxEmptyRetries` y con `maxVariants`, prompt endurecido, (opcional) `output_config.format` |
| `src/sourcing/supplierSource.test.ts` | Modificar | Tests de variantes, fan-out/dedup, budget actualizado, prompt endurecido |
| `api/index.ts` | Modificar | `VERCEL_SEARCH_BUDGET` sin `maxEmptyRetries` |
| `src/directory/store.ts` | Solo lectura | Exporta `supplierKey(candidate)` (línea 28) — se importa, no se toca |

---

### Task 1: `buildQueryVariants` — variantes fijas de la query

**Files:**
- Modify: `src/sourcing/supplierSource.ts`
- Test: `src/sourcing/supplierSource.test.ts`

- [ ] **Step 1: Write the failing test**

Agregar al final de `src/sourcing/supplierSource.test.ts` (nivel superior, fuera de los `describe` existentes). Agregar `buildQueryVariants` al import de la línea 2:

```ts
import { createSupplierSource, buildQueryVariants } from "./supplierSource.js";
```

```ts
describe("buildQueryVariants", () => {
  it("genera 3 variantes fijas con la query original primero", () => {
    // Arrange
    const query = "dinamómetro Extech 475040";

    // Act
    const variants = buildQueryVariants(query);

    // Assert: plantillas fijas del spec, la original SIEMPRE primera.
    expect(variants).toEqual([
      "dinamómetro Extech 475040",
      "distribuidor mayorista de dinamómetro Extech 475040 en México",
      "proveedores de dinamómetro Extech 475040 al por mayor",
    ]);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `rtk vitest run src/sourcing/supplierSource.test.ts`
Expected: FAIL — `buildQueryVariants` no está exportada (error de import/undefined).

- [ ] **Step 3: Write minimal implementation**

En `src/sourcing/supplierSource.ts`, debajo de las constantes (después de la línea de `MAX_ENRICH_SEARCH_USES`):

```ts
/**
 * Variantes fijas de la búsqueda para el fan-out (sin gastar tokens en
 * generarlas). La primera es SIEMPRE la query tal cual la escribió el usuario.
 */
export function buildQueryVariants(query: string): readonly string[] {
  return [
    query,
    `distribuidor mayorista de ${query} en México`,
    `proveedores de ${query} al por mayor`,
  ];
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `rtk vitest run src/sourcing/supplierSource.test.ts`
Expected: PASS (todos los tests, incluidos los preexistentes).

- [ ] **Step 5: Commit**

```bash
rtk git add src/sourcing/supplierSource.ts src/sourcing/supplierSource.test.ts
rtk git commit -m "feat: buildQueryVariants con 3 plantillas fijas para el fan-out"
```

---

### Task 2: Fan-out en `search()` + dedup por `supplierKey` + `maxVariants` (adiós `maxEmptyRetries`)

Reescribe `search()` para correr las variantes en paralelo con `Promise.allSettled`, deduplicar por `supplierKey` y eliminar el reintento-si-vacío. Es UN cambio coherente: el retry y el fan-out son implementaciones alternativas del mismo loop, no pueden convivir.

**Files:**
- Modify: `src/sourcing/supplierSource.ts` (interfaz `SearchBudget`, constantes, `search()`)
- Modify: `src/sourcing/supplierSource.test.ts` (reemplaza el describe del retry; actualiza los de budget)
- Modify: `api/index.ts:17-22` (`VERCEL_SEARCH_BUDGET` sin `maxEmptyRetries`)

- [ ] **Step 1: Write the failing tests**

En `src/sourcing/supplierSource.test.ts`:

**1a.** Reemplazar el helper `fakeClientSequence` (líneas 5-22) por una versión que también soporte rechazos por llamada (`fakeClient` y `fakeClientSequence` se conservan encima de la nueva base, para no tocar el resto de tests):

```ts
// Cliente Anthropic mínimo mockeado: solo messages.create.
// Un resultado por llamada, en orden: texto de respuesta o rechazo (error).
// Repite el último resultado si se agotan.
type FakeOutcome = { text: string } | { reject: string };

function fakeClientOutcomes(outcomes: readonly FakeOutcome[]) {
  let call = 0;
  const create = vi.fn(async (_params: unknown) => {
    const outcome = outcomes[Math.min(call, outcomes.length - 1)] ?? { text: "" };
    call += 1;
    if ("reject" in outcome) {
      throw new Error(outcome.reject);
    }
    return {
      stop_reason: "end_turn",
      content: [{ type: "text", text: outcome.text }],
    };
  });
  return { client: { messages: { create } } as never, create };
}

function fakeClientSequence(texts: readonly string[]) {
  return fakeClientOutcomes(texts.map((text) => ({ text })));
}

function fakeClient(text: string) {
  return fakeClientSequence([text]).client;
}
```

**1b.** ELIMINAR completo el `describe("reintento si la búsqueda viene vacía", ...)` (líneas 56-92) y en su lugar agregar:

```ts
describe("fan-out de variantes", () => {
  const EMPTY = JSON.stringify({ suppliers: [] });
  // Otro proveedor (web distinta → supplierKey distinto al de RESPONSE).
  const OTHER_RESPONSE = JSON.stringify({
    suppliers: [
      { name: "Ferretera MTY", website: "https://ferretera.mx", material: "lámina" },
    ],
  });

  it("dispara una llamada por variante y cada prompt lleva su variante", async () => {
    // Arrange
    const { client, create } = fakeClientOutcomes([
      { text: RESPONSE },
      { text: EMPTY },
      { text: EMPTY },
    ]);
    const source = createSupplierSource({ client });

    // Act
    await source.search({ query: "lámina galvanizada", region: "mx" });

    // Assert: 3 llamadas (default) y los prompts interpolan cada variante.
    expect(create).toHaveBeenCalledTimes(3);
    type Call = { messages: { content: string }[] };
    const prompts = create.mock.calls.map((c) => (c[0] as Call).messages[0]?.content ?? "");
    expect(prompts[0]).toContain('"lámina galvanizada"');
    expect(prompts[1]).toContain('"distribuidor mayorista de lámina galvanizada en México"');
    expect(prompts[2]).toContain('"proveedores de lámina galvanizada al por mayor"');
  });

  it("combina los resultados y deduplica por supplierKey (el primero gana)", async () => {
    // Arrange: variantes 1 y 2 devuelven el MISMO proveedor (misma web); la 3, otro.
    const { client } = fakeClientOutcomes([
      { text: RESPONSE },
      { text: RESPONSE },
      { text: OTHER_RESPONSE },
    ]);
    const source = createSupplierSource({ client });

    // Act
    const result = await source.search({ query: "lámina", region: "mx" });

    // Assert: 2 únicos, sin duplicar Aceros del Norte.
    expect(result).toHaveLength(2);
    expect(result.map((s) => s.name).sort()).toEqual(["Aceros del Norte", "Ferretera MTY"]);
  });

  it("una variante que falla no tumba el resultado (se usan las otras)", async () => {
    const { client } = fakeClientOutcomes([
      { reject: "boom" },
      { text: RESPONSE },
      { text: EMPTY },
    ]);
    const source = createSupplierSource({ client });

    const result = await source.search({ query: "lámina", region: "mx" });

    expect(result).toHaveLength(1);
    expect(result[0]?.name).toBe("Aceros del Norte");
  });

  it("propaga un error cuando TODAS las variantes fallan", async () => {
    const { client } = fakeClientOutcomes([
      { reject: "boom" },
      { reject: "boom" },
      { reject: "boom" },
    ]);
    const source = createSupplierSource({ client });

    await expect(source.search({ query: "x", region: "mx" })).rejects.toThrow("boom");
  });

  it("con maxVariants: 1 hace una sola llamada (comportamiento anterior)", async () => {
    const { client, create } = fakeClientOutcomes([{ text: RESPONSE }]);
    const source = createSupplierSource({
      client,
      searchBudget: { maxWebSearchUses: 2, maxTokens: 8000, maxVariants: 1 },
    });

    const result = await source.search({ query: "lámina", region: "mx" });

    expect(create).toHaveBeenCalledTimes(1);
    expect(result).toHaveLength(1);
  });
});
```

**1c.** Actualizar el `describe("createSupplierSource — searchBudget", ...)` (los dos tests cambian porque `maxEmptyRetries` desaparece):

```ts
describe("createSupplierSource — searchBudget", () => {
  const emptyJson = JSON.stringify({ suppliers: [] });

  // Forma mínima de los argumentos de messages.create que asertamos.
  type CreateArgs = {
    max_tokens: number;
    thinking: unknown;
    output_config?: unknown;
    tools: { max_uses: number }[];
  };

  it("sin searchBudget usa los defaults (adaptive sin effort, 5 usos, 3 variantes)", async () => {
    const { client, create } = fakeClientSequence([emptyJson]);
    const source = createSupplierSource({ client });
    await source.search({ query: "láminas", region: "mx" });
    // Default de fan-out: 3 variantes → 3 llamadas.
    expect(create).toHaveBeenCalledTimes(3);
    const args = create.mock.calls[0]?.[0] as CreateArgs;
    expect(args.thinking).toEqual({ type: "adaptive" });
    expect(args.output_config).toBeUndefined();
    expect(args.tools[0]?.max_uses).toBe(5);
  });

  it("el budget (effort/maxTokens/maxWebSearchUses) se aplica a CADA llamada del fan-out", async () => {
    const { client, create } = fakeClientSequence([emptyJson]);
    const source = createSupplierSource({
      client,
      searchBudget: { maxWebSearchUses: 2, maxTokens: 8000, effort: "low" },
    });
    await source.search({ query: "láminas", region: "mx" });
    // Sin maxVariants explícito el default sigue siendo 3.
    expect(create).toHaveBeenCalledTimes(3);
    for (const call of create.mock.calls) {
      const args = call[0] as CreateArgs;
      // El modelo (opus 4.8) NO soporta thinking "enabled": adaptive + effort.
      expect(args.thinking).toEqual({ type: "adaptive" });
      expect(args.output_config).toEqual({ effort: "low" });
      expect(args.max_tokens).toBe(8000);
      expect(args.tools[0]?.max_uses).toBe(2);
    }
  });
});
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `rtk vitest run src/sourcing/supplierSource.test.ts`
Expected: FAIL — errores de tipo (`maxVariants` no existe en `SearchBudget`; falta `maxEmptyRetries` requerido) y/o conteos de llamadas incorrectos (2 en vez de 3).

- [ ] **Step 3: Write the implementation**

En `src/sourcing/supplierSource.ts`:

**3a.** Import de `supplierKey` (junto a los imports existentes; `store.ts` no importa de `sourcing/`, no hay ciclo):

```ts
import { supplierKey } from "../directory/store.js";
```

**3b.** Constantes: eliminar `MAX_EMPTY_RETRIES` (línea 14-15) y agregar en su lugar:

```ts
// Variantes de query por búsqueda (fan-out en paralelo).
const MAX_VARIANTS = 3;
```

**3c.** `SearchBudget`: eliminar `maxEmptyRetries` y agregar `maxVariants`:

```ts
/** Presupuesto opcional para acotar la búsqueda (deploy con límite de tiempo). */
export interface SearchBudget {
  maxWebSearchUses: number;
  maxTokens: number;
  /** Cuántas variantes de query corre el fan-out (default 3; con 1 no hay fan-out). */
  maxVariants?: number;
  /**
   * Nivel de esfuerzo de razonamiento (`output_config.effort`). El modelo
   * (opus 4.8) usa thinking "adaptive" + effort para controlar cuánto piensa;
   * NO soporta thinking "enabled" con budget. Si se omite, no se acota el effort.
   */
  effort?: "low" | "medium" | "high";
}
```

**3d.** En `createSupplierSource`, reemplazar la línea de `maxEmptyRetries` por:

```ts
  const maxVariants = deps.searchBudget?.maxVariants ?? MAX_VARIANTS;
```

**3e.** Reemplazar la función `search` completa (el loop de reintentos, líneas 205-221) por:

```ts
  // Fan-out: corre las variantes de la query en paralelo y une los resultados.
  // Una variante que falla no tumba a las demás; si TODAS fallan, se propaga.
  async function search(q: SupplierQuery): Promise<readonly SupplierCandidate[]> {
    const variants = buildQueryVariants(q.query).slice(0, maxVariants);
    const settled = await Promise.allSettled(
      variants.map((variant) => searchOnce({ query: variant, region: q.region })),
    );

    const failures = settled.filter((r): r is PromiseRejectedResult => r.status === "rejected");
    for (const failure of failures) {
      logger.warn("sourcing: una variante del fan-out falló", {
        query: q.query,
        region: q.region,
        reason: failure.reason instanceof Error ? failure.reason.message : String(failure.reason),
      });
    }
    if (failures.length === settled.length) {
      const reason = failures[0]?.reason;
      throw reason instanceof Error ? reason : new Error(String(reason));
    }

    // Unión deduplicada con la misma identidad del directorio: el primero gana.
    const byKey = new Map<string, SupplierCandidate>();
    for (const result of settled) {
      if (result.status !== "fulfilled") {
        continue;
      }
      for (const candidate of result.value) {
        if (!byKey.has(supplierKey(candidate))) {
          byKey.set(supplierKey(candidate), candidate);
        }
      }
    }
    return [...byKey.values()];
  }
```

**3f.** En `api/index.ts` (líneas 17-22), quitar la línea `maxEmptyRetries: 0,`:

```ts
const VERCEL_SEARCH_BUDGET: SearchBudget = {
  maxWebSearchUses: 2,
  maxTokens: 8000,
  effort: "low",
};
```

(El default `maxVariants: 3` aplica también en Vercel: las llamadas son concurrentes, el wall-clock ≈ la variante más lenta.)

- [ ] **Step 4: Run tests to verify they pass**

Run: `rtk vitest run src/sourcing/supplierSource.test.ts`
Expected: PASS. Luego `rtk pnpm typecheck` → sin errores (confirma que `api/index.ts` y `scripts/repoblar.ts` compilan con el nuevo `SearchBudget`).

- [ ] **Step 5: Run full suite**

Run: `rtk pnpm test`
Expected: PASS (nada más consumía `maxEmptyRetries`).

- [ ] **Step 6: Commit**

```bash
rtk git add src/sourcing/supplierSource.ts src/sourcing/supplierSource.test.ts api/index.ts
rtk git commit -m "feat: fan-out de 3 variantes en paralelo con dedup por supplierKey (reemplaza maxEmptyRetries)"
```

---

### Task 3: Prompt endurecido — precios y dirección SIEMPRE a campos estructurados

**Files:**
- Modify: `src/sourcing/supplierSource.ts` (`buildSystemPrompt`)
- Test: `src/sourcing/supplierSource.test.ts`

- [ ] **Step 1: Write the failing test**

Agregar dentro del `describe("createSupplierSource", ...)` principal (junto a los primeros tests de `search`):

```ts
  it("el system prompt exige volcar precios y dirección a campos estructurados, no a notes", async () => {
    // Arrange
    const { client, create } = fakeClientSequence([RESPONSE]);
    const source = createSupplierSource({ client });

    // Act
    await source.search({ query: "x", region: "mx" });

    // Assert: la regla y el mini-ejemplo correcto/incorrecto están en el prompt.
    const call = create.mock.calls[0]?.[0] as { system: string };
    expect(call.system).toContain('NUNCA dejes un precio o una dirección solo en "notes"');
    expect(call.system).toContain("Incorrecto:");
    expect(call.system).toContain("Correcto:");
  });
```

- [ ] **Step 2: Run test to verify it fails**

Run: `rtk vitest run src/sourcing/supplierSource.test.ts`
Expected: FAIL — el system prompt actual no contiene esos textos.

- [ ] **Step 3: Write the implementation**

En `buildSystemPrompt()`, después de las líneas existentes de `catalogPrice`/`address` ("...omitilos si no están.") y antes de la línea de `trusted`, insertar:

```ts
    'REGLA DE CAMPOS: NUNCA dejes un precio o una dirección solo en "notes".',
    'Todo precio hallado va en "wholesalePrice" (si es de volumen/mayoreo) o en',
    '"catalogPrice" (si es de lista/catálogo), siempre con su "currency"; toda',
    'dirección o ciudad va en "address". "notes" es solo para contexto extra.',
    'Incorrecto: { "name": "X", "notes": "precio de lista $439.99, en Monterrey" }.',
    'Correcto: { "name": "X", "catalogPrice": 439.99, "currency": "USD",',
    '  "address": "Monterrey, NL", "notes": "precio publicado en su catálogo" }.',
```

- [ ] **Step 4: Run test to verify it passes**

Run: `rtk vitest run src/sourcing/supplierSource.test.ts`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
rtk git add src/sourcing/supplierSource.ts src/sourcing/supplierSource.test.ts
rtk git commit -m "feat: prompt endurecido, precios y dirección van a campos estructurados"
```

---

### Task 4: Salida estructurada de la API (mejor esfuerzo, con verificación en vivo)

El spec lo define como **mejor esfuerzo**: primero se verifica con UNA llamada real y barata si `output_config.format` (JSON schema forzado) convive con el server tool `web_search` en `claude-opus-4-8`. Según la referencia vigente del SDK, structured outputs es incompatible con citations y prefilling, pero la combinación con server tools no está documentada explícitamente → hay que probarla. La decisión y su evidencia van al PR.

**Files:**
- Create (scratchpad, NO se commitea): `<scratchpad>/verify-structured-output.ts`
- Modify (solo si es compatible): `src/sourcing/supplierSource.ts`, `src/sourcing/supplierSource.test.ts`

- [ ] **Step 1: Script de verificación en vivo (scratchpad)**

Crear `verify-structured-output.ts` en el directorio scratchpad de la sesión (NUNCA dentro del repo):

```ts
// Verifica si output_config.format convive con web_search en claude-opus-4-8.
// Uso: pnpm dlx tsx <scratchpad>/verify-structured-output.ts (corre desde la raíz del repo, usa su .env)
import Anthropic from "@anthropic-ai/sdk";
import { loadDotenvIfPresent } from "/Users/zaric/Code/comparador-compras/src/config/loadDotenv.js";

loadDotenvIfPresent();
const client = new Anthropic({ apiKey: process.env.ANTHROPIC_API_KEY });

const response = await client.messages.create({
  model: "claude-opus-4-8",
  max_tokens: 2000,
  thinking: { type: "adaptive" },
  output_config: {
    effort: "low",
    format: {
      type: "json_schema",
      schema: {
        type: "object",
        properties: {
          suppliers: {
            type: "array",
            items: {
              type: "object",
              properties: { name: { type: "string" } },
              required: ["name"],
              additionalProperties: false,
            },
          },
        },
        required: ["suppliers"],
        additionalProperties: false,
      },
    },
  },
  tools: [{ type: "web_search_20260209", name: "web_search", max_uses: 1 }],
  messages: [
    { role: "user", content: "Buscá UN proveedor de tornillos en México y devolvé el JSON." },
  ],
});

process.stdout.write(`${JSON.stringify({ stop: response.stop_reason, content: response.content.at(-1) }, null, 2)}\n`);
```

Nota: si el tipo `output_config.format` no existe en la versión instalada del SDK (^0.105.0), eso ya es evidencia de fricción → rama B.

Run: `cd /Users/zaric/Code/comparador-compras && pnpm dlx tsx <scratchpad>/verify-structured-output.ts`
Expected: o bien un JSON válido con `suppliers` (compatible → **rama A**), o un 400/error de tipos (incompatible → **rama B**).

- [ ] **Step 2A (SOLO si compatible): failing test del formato forzado**

Agregar al `describe("createSupplierSource — searchBudget", ...)` o en un describe propio:

```ts
  it("la búsqueda fuerza el schema de suppliers con output_config.format", async () => {
    const { client, create } = fakeClientSequence([RESPONSE]);
    const source = createSupplierSource({ client });

    await source.search({ query: "x", region: "mx" });

    type Args = { output_config?: { format?: { type: string } } };
    const args = create.mock.calls[0]?.[0] as Args;
    expect(args.output_config?.format?.type).toBe("json_schema");
  });
```

OJO: agregar `format` a `output_config` rompe DOS asserts existentes del describe de budget — actualizarlos así:
- "sin searchBudget usa los defaults": `expect(args.output_config).toBeUndefined()` → asertar `(args.output_config as { effort?: string; format?: { type: string } }).effort` es `undefined` y `.format?.type === "json_schema"`.
- "el budget ... se aplica a CADA llamada": `expect(args.output_config).toEqual({ effort: "low" })` → asertar `.effort === "low"` y `.format?.type === "json_schema"`.

Run: `rtk vitest run src/sourcing/supplierSource.test.ts` → FAIL.

- [ ] **Step 3A (SOLO si compatible): implementación**

En `supplierSource.ts`, agregar el schema (constante a nivel módulo, espejo de `parseSuppliers`; el parseo defensivo con zod QUEDA como red de seguridad):

```ts
// Schema JSON forzado de la respuesta (espejo de supplierSchema). El parseo
// defensivo con zod queda como red de seguridad aunque el formato sea forzado.
const SUPPLIERS_OUTPUT_FORMAT = {
  type: "json_schema",
  schema: {
    type: "object",
    properties: {
      suppliers: {
        type: "array",
        items: {
          type: "object",
          properties: {
            name: { type: "string" },
            website: { type: "string" },
            material: { type: "string" },
            wholesalePrice: { type: "number" },
            currency: { type: "string" },
            moq: { type: "number" },
            priceUnit: { type: "string", enum: ["pieza", "kg", "tonelada", "m2"] },
            availability: { type: "string", enum: ["disponible", "sobre pedido"] },
            catalogPrice: { type: "number" },
            address: { type: "string" },
            contact: {
              type: "object",
              properties: {
                email: { type: "string" },
                phone: { type: "string" },
                whatsapp: { type: "string" },
                formUrl: { type: "string" },
              },
              additionalProperties: false,
            },
            trusted: { type: "boolean" },
            notes: { type: "string" },
          },
          required: ["name", "material"],
          additionalProperties: false,
        },
      },
    },
    required: ["suppliers"],
    additionalProperties: false,
  },
} as const;
```

Y en `searchOnce`, reemplazar el spread condicional de `output_config`:

```ts
      // El formato forzado garantiza el JSON; effort solo si el budget lo acota.
      output_config: {
        format: SUPPLIERS_OUTPUT_FORMAT,
        ...(effort !== undefined ? { effort } : {}),
      },
```

(El `enrichContact` NO cambia — el spec lo excluye.)

Run: `rtk vitest run src/sourcing/supplierSource.test.ts` → PASS. `rtk pnpm typecheck` → sin errores.

- [ ] **Step 4A: Commit**

```bash
rtk git add src/sourcing/supplierSource.ts src/sourcing/supplierSource.test.ts
rtk git commit -m "feat: salida estructurada (output_config.format) en la búsqueda, zod queda de red de seguridad"
```

- [ ] **Step 2B (SOLO si incompatible): documentar la decisión**

Editar `docs/superpowers/specs/2026-07-16-v2.4-fanout-extraccion-design.md`, sección "2. Extracción estructurada": agregar al final una línea `> Verificado el 2026-07-16: <error/fricción observada literal>. Se adopta SOLO el prompt endurecido (plan B del spec).` Commit:

```bash
rtk git add docs/superpowers/specs/2026-07-16-v2.4-fanout-extraccion-design.md
rtk git commit -m "docs: salida estructurada descartada (evidencia de incompatibilidad con web_search)"
```

- [ ] **Step 5: Borrar el script del scratchpad** (es temporal; no debe quedar referencia en el repo).

---

### Task 5: Verificación integral

**Files:** ninguno nuevo — solo verificación.

- [ ] **Step 1: Suite completa + calidad**

```bash
rtk pnpm test && rtk pnpm typecheck && rtk pnpm lint && rtk pnpm format:check
```

Expected: todo verde. Si `format:check` falla: `rtk pnpm format` y amend/commit `chore: formato`.

- [ ] **Step 2: Smoke test real de búsqueda (opcional pero recomendado, ~3x tokens)**

```bash
cd /Users/zaric/Code/comparador-compras && pnpm dlx tsx scripts/repoblar.ts "Dinamómetro digital Extech 475040"
```

Expected: corre las 3 variantes, reporta `N hallados · M nuevos` con N ≥ lo típico de una sola query, y (si Task 4A aplicó o el prompt endurecido funciona) más candidatos con `catalogPrice`/`address`. OJO: esto ESCRIBE en el Redis de producción vía merge seguro (preserva favoritos/estados) — es el mismo flujo ya usado en el re-poblado.

- [ ] **Step 3: Estado final**

`rtk git log --oneline main..HEAD` — confirmar la serie de commits. **NO pushear ni abrir PR**: eso lo ordena el usuario explícitamente (regla del proyecto).

---

## Criterios de éxito (del spec)

- Una búsqueda dispara 3 llamadas en paralelo y devuelve la unión deduplicada.
- `maxVariants: 1` reproduce el comportamiento anterior (una llamada, sin retry).
- El budget (`effort`/`maxTokens`/`maxWebSearchUses`) se aplica a cada llamada.
- El contrato de `/api/buscar` no cambia (mismos campos de respuesta).
- El system prompt contiene la regla de campos obligatorios con ejemplo.
- La decisión sobre salida estructurada queda documentada con evidencia.
