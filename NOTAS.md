# Notas — comparador-compras

Escribe aquí ideas en lenguaje natural, sin formato. No te preocupes por
prioridad ni etiquetas. En la siguiente corrida, el sistema autopilot las lee,
las convierte en tareas del `BACKLOG.md` (etiquetando lo ambiguo como `[review]`)
y borra de aquí lo que ya procesó.

---

<!-- 2026-08-02: procesada la nota fundacional del proyecto (agente que busca
     productos, valida proveedores, compara precios y sugiere la mejor opción /
     upgrade). Todo lo pedido se construyó: la comparación retail + upgrade fue
     el CLI v1 (archivado en el tag legacy-cli-v1) y la validación de
     proveedores con mejor opción es la app v2 en producción. -->
