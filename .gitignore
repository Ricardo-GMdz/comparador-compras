.env
.env.*
# El template de ejemplo sí se versiona (no tiene secretos).
!.env.example
node_modules/
.DS_Store
dist/
# Salida del build de Vercel (regenerada desde web/ por build:vercel).
public/
.vercel/
coverage/
.superpowers/
directorio.json
